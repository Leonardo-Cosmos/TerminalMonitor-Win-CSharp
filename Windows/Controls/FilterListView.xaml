<UserControl x:Class="TerminalMonitor.Windows.Controls.FilterListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TerminalMonitor.Windows.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:FilterItemVO}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Label Name="lblField" Grid.Row="0" Grid.Column="0" Content="Key" />
        <TextBox Name="txtBxField" Grid.Row="0" Grid.Column="1" Text="{Binding Path=FieldKey}" />

        <ComboBox Name="cmbBxOperator" Grid.Row="0" Grid.Column="2" SelectedValue="{Binding CheckOperator}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource CheckOperatorToStringConverter}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <Grid Name="grdButton" Grid.Row="0" Grid.Column="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Name="btnAdd" Grid.Column="0" Click="BtnAdd_Click" Content="Add" />
            <Button Name="btnUpdate" Grid.Column="1" Click="BtnUpdate_Click" Content="Update" />
            <Button Name="btnDelete" Grid.Column="2" Click="BtnDelete_Click" Content="Delete" />
        </Grid>

        <Label Name="lblValue" Grid.Row="1" Grid.Column="0" Content="Value" />
        <TextBox Name="txtBxValue" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding ComparedValue}" />

        <ListView Name="lstFilters" Grid.Row="3" Grid.ColumnSpan="4" SelectionMode="Single"
                  SelectionChanged="LstFilters_SelectionChanged">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel></WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="SkyBlue" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding FieldKey}" Margin="3" />
                            <TextBlock Text="{Binding CheckOperator, Converter={StaticResource CheckOperatorToStringConverter}}" Margin="3" />
                            <TextBlock Text="{Binding ComparedValue}" Margin="3" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
